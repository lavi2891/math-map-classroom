export type ClassSummary = {
  id: string;
  name: string;
  studentCount: number;
  focus: string;
};

export type ClassFeedItem = {
  id: string;
  title: string;
  description: string;
  actionLabel: string;
};
